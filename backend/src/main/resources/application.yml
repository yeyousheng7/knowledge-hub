spring:
  application:
    name: knowledgehub
  ai:
    model:
      chat: ${SPRING_AI_MODEL_CHAT:none}
      embedding: ${SPRING_AI_MODEL_EMBEDDING:none}
      image: none
      audio:
        speech: none
        transcription: none
      moderation: none
    openai:
      chat:
        base-url: ${AI_CHAT_BASE_URL:}
        api-key: ${AI_CHAT_API_KEY:}
        options:
          model: ${AI_CHAT_MODEL:}
      embedding:
        base-url: ${AI_EMBEDDING_BASE_URL:}
        api-key: ${AI_EMBEDDING_API_KEY:}
        options:
          model: ${AI_EMBEDDING_MODEL:}
          dimensions: ${AI_EMBEDDING_DIMENSIONS:1024}
    vectorstore:
      redis:
        initialize-schema: ${AI_VECTORSTORE_REDIS_INITIALIZE_SCHEMA:false}
        index-name: ${AI_INDEX_VECTOR_INDEX_NAME:kh_note_chunks}
        prefix: "${AI_VECTORSTORE_REDIS_PREFIX:kh:ai:note:chunk:}"
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      database: ${REDIS_DATABASE:0}

server:
  port: 8080

app:
  invite-code: ${APP_INVITE_CODE}
  jwt:
    secret: ${JWT_SECRET}
    expire-seconds: ${JWT_EXPIRE_SECONDS:86400}
  ai:
    enabled: ${AI_ENABLED:false}
    chat:
      provider: ${AI_CHAT_PROVIDER:deepseek}
    embedding:
      dimensions: ${AI_EMBEDDING_DIMENSIONS:1024}
    index:
      chunk-size: ${AI_INDEX_CHUNK_SIZE:1024}
      chunk-overlap: ${AI_INDEX_CHUNK_OVERLAP:50}
      top-k: ${AI_INDEX_TOP_K:3}
      vector-index-name: ${AI_INDEX_VECTOR_INDEX_NAME:kh_note_chunks}
      vector-store: ${AI_INDEX_VECTOR_STORE:none}
    rag:
      enabled: ${AI_RAG_ENABLED:false}
    agent:
      enabled: ${AI_AGENT_ENABLED:false}
